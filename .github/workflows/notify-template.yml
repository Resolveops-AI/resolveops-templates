name: Reusable Notification

on:
  workflow_call:
    inputs:
      service_name:
        description: "Name of the service"
        required: true
        type: string
      environment:
        description: "Target environment"
        required: true
        type: string
      failed_job_name:
        description: "Name of the failed job"
        required: false
        type: string
      status:
        description: "Status of the pipeline (e.g., success, failure)"
        required: true
        type: string
      run_url:
        description: "GitHub Actions run URL"
        required: true
        type: string
      branch_name:
        description: "Branch name"
        required: true
        type: string
      commit_sha:
        description: "Commit SHA"
        required: true
        type: string
      actor:
        description: "Actor who triggered the run"
        required: true
        type: string
      workflow_name:
        description: "Workflow name"
        required: true
        type: string

    secrets:
      WEBHOOK_URL:
        required: false
      SMTP_HOST:
        required: false
      SMTP_PORT:
        required: false
      SMTP_USERNAME:
        required: false
      SMTP_PASSWORD:
        required: false
      EMAIL_FROM:
        required: false
      EMAIL_TO:
        required: false

jobs:
  notify:
    name: Pipeline Notification
    runs-on: ubuntu-latest
    if: always()
    env:
      WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      SMTP_HOST: ${{ secrets.SMTP_HOST }}
      SMTP_PORT: ${{ secrets.SMTP_PORT }}
      SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
      EMAIL_TO: ${{ secrets.EMAIL_TO }}
    steps:
      - name: GitHub Step Summary Notification
        run: |
          echo "## Pipeline Status: ${{ inputs.status }}" >> $GITHUB_STEP_SUMMARY
          echo "**Service:** ${{ inputs.service_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** ${{ inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          if [ -n "${{ inputs.failed_job_name }}" ]; then
            echo "**Failed Job:** ${{ inputs.failed_job_name }}" >> $GITHUB_STEP_SUMMARY
          fi
          echo "**Branch:** ${{ inputs.branch_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ inputs.commit_sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Actor:** ${{ inputs.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow:** ${{ inputs.workflow_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Run URL:** ${{ inputs.run_url }}" >> $GITHUB_STEP_SUMMARY

      - name: Send Webhook Notification
        if: env.WEBHOOK_URL != ''
        run: |
          curl -X POST "${{ env.WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d '{
              "service": "${{ inputs.service_name }}",
              "status": "${{ inputs.status }}",
              "environment": "${{ inputs.environment }}",
              "failed_job": "${{ inputs.failed_job_name }}",
              "branch": "${{ inputs.branch_name }}",
              "actor": "${{ inputs.actor }}",
              "run_url": "${{ inputs.run_url }}"
            }' || true

      - name: Send Email Notification
        if: env.SMTP_HOST != '' && env.SMTP_PORT != '' && env.SMTP_USERNAME != '' && env.SMTP_PASSWORD != '' && env.EMAIL_FROM != '' && env.EMAIL_TO != ''
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ env.SMTP_HOST }}
          server_port: ${{ env.SMTP_PORT }}
          username: ${{ env.SMTP_USERNAME }}
          password: ${{ env.SMTP_PASSWORD }}
          subject: "[${{ inputs.status }}] ${{ inputs.workflow_name }} for ${{ inputs.service_name }}"
          to: ${{ env.EMAIL_TO }}
          from: ${{ env.EMAIL_FROM }}
          body: |
            Pipeline Status: ${{ inputs.status }}
            Service: ${{ inputs.service_name }}
            Environment: ${{ inputs.environment }}
            Failed Job: ${{ inputs.failed_job_name }}
            Branch: ${{ inputs.branch_name }}
            Commit: ${{ inputs.commit_sha }}
            Actor: ${{ inputs.actor }}
            Workflow: ${{ inputs.workflow_name }}
            Run URL: ${{ inputs.run_url }}
        continue-on-error: true
